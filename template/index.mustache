<!doctype html>
<html lang="en">
    <head>
        {{>head}}
        <link rel="stylesheet" href="/res/index.css">
    </head>
    <body>
        <h1>{{title}}: <span class="status-{{status_class}}">{{status_text}}</span></h1>
        <div>
            <p id="version">Agent version: {{version}}
                {{#ssh_host}}
                <button class="update">Check updates</button>
                {{/ssh_host}}
            </p>
            <p id="scrape">Latest data collection: {{{scrape_date}}}
                {{#export_url}}
                (<a href="{{export_url}}">view</a>)
                {{/export_url}}
            </p>
            <p id="edit"><a href="/edit">Edit configuration</a></p>
            <p id="link">Find out where the collected data is used for in the <a href="http://visualization.gros.example">GROS Visualization Hub</a></p>
        </div>
        <script src="/jquery/jquery.min.js"></script>
        <script src="/res/index.js"></script>
    </body>
</html>
