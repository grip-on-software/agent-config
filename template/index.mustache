<!doctype html>
<html lang="en">
    <head>
		<meta charset="utf-8">
        <title>Agent status | GROS</title>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto|Vollkorn">
        <link rel="stylesheet" href="/res/index.css">
    </head>
    <body>
        <h1>Agent status: <span class="status-{{status_class}}">{{status_text}}</span></h1>
		<div>
			<p>Agent version: {{version}}</p>
			<p>Latest data collection: {{{scrape_date}}}
				{{#export_url}}
				(<a href="{{export_url}}">view</a>)
				{{/export_url}}
			</p>
			<p><a href="/edit">Edit configuration</a></p>
			<p>Find out where the collected data is used for in the <a href="http://visualization.gros.example">GROS Visualization Hub</a></p>
		</div>
    </body>
</html>
